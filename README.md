Mac setup using https://github.com/zero-sh/zero.sh

Set up by running `setup.sh <workspace>` where `<workspace>` is directory under
`workspaces/`

Note I don't use `default.yaml` as it is too constraining, defaults are set via
scripts in `run`.
